{
  "version": "1.0",
  "rules": [
    {
      "origin": ["http://localhost:5173"],
      "method": ["GET", "POST", "PUT", "DELETE", "HEAD"],
      "responseHeader": ["Content-Type", "ETag", "x-amz-server-side-encryption"],
      "maxAgeSeconds": 3000
    }
  ]
}
